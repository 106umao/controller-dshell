---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: dshells.dshell.smartx.com
spec:
  group: dshell.smartx.com
  names:
    kind: DShell
    listKind: DShellList
    plural: dshells
    singular: dshell
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: DShell is the Schema for the dshells API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DShellSpec defines the desired state of DShell
            properties:
              command:
                description: Command client update this field to distribute shell
                  command event.
                type: string
            type: object
          status:
            description: DShellStatus defines the observed state of DShell
            properties:
              command:
                description: Command to execute in local env.
                type: string
              nodesResults:
                description: NodesResults is distribution shell execution result histories.
                items:
                  properties:
                    EndTime:
                      description: EndTime command execution time.
                      format: date-time
                      type: string
                    StartTime:
                      description: StartTime command execution time.
                      format: date-time
                      type: string
                    controllerPodIp:
                      description: ControllerPodIp
                      type: string
                    message:
                      description: Message is shell execution result of current nodes.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
