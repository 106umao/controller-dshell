---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: dshells.dshell.smartx.com
spec:
  group: dshell.smartx.com
  names:
    kind: DShell
    listKind: DShellList
    plural: dshells
    singular: dshell
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: DShell is the Schema for the dshells API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DShellSpec defines the desired state of DShell
            properties:
              command:
                description: Command client update this field to distribute shell
                  command event.
                type: string
            type: object
          status:
            description: DShellStatus defines the observed state of DShell
            properties:
              executeHistories:
                description: ExecuteHistories is distribution shell execution result
                  histories.
                items:
                  properties:
                    command:
                      description: Command shell command from CR event.
                      type: string
                    executeTime:
                      description: ExecuteTime command execution time.
                      format: date-time
                      type: string
                    nodeExecResults:
                      description: NodeExecResults command relation to result is 1
                        to N.
                      items:
                        properties:
                          addresses:
                            description: Addresses is nodes ip info of k8s cluster.
                            items:
                              description: NodeAddress contains information for the
                                node's address.
                              properties:
                                address:
                                  description: The node address.
                                  type: string
                                type:
                                  description: Node address type, one of Hostname,
                                    ExternalIP or InternalIP.
                                  type: string
                              required:
                              - address
                              - type
                              type: object
                            type: array
                          message:
                            description: Message is shell execution result of current
                              nodes.
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              lastCommand:
                description: LastCommand is executed last time.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
